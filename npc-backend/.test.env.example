# 测试环境变量示例
# 复制此文件为 .test.env 并填入实际值（如果需要真实数据库测试）

NODE_ENV=test
PORT=8001

# 数据库配置（测试环境）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_test_password
DB_NAME=npc_db_test

# JWT 配置
JWT_SECRET=test-secret-key-change-in-production

# LLM API 配置（可选，用于集成测试）
ENABLE_OPENROUTER=false
OPENROUTER_API_KEY=sk-test-key

