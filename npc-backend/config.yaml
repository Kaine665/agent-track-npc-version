# ============================================
# NPC Backend 配置文件
# ============================================
# 说明：使用 YAML 格式存储配置，比 .env 更清晰易读
# 最后更新：2025-11-21

# 服务器配置
server:
  port: 8000

# 数据库配置
database:
  host: localhost
  port: 3306
  user: root
  password: "j877413fxt"  # ⚠️ 请填写你的 MySQL root 密码
  name: npc_db

# LLM 提供商配置
llm:
  # OpenRouter 配置（推荐，统一使用 OpenRouter）
  # 支持多个 API Key，用逗号分隔，系统会自动故障转移
  # 当第一个 API Key 失败（401/403/429/超时）时，会自动尝试下一个
  openrouter:
    enabled: true
    api_key: "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,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"
  
  # OpenAI 配置（可选）
  openai:
    enabled: false
    api_key: ""
  
  # DeepSeek 配置（可选）
  deepseek:
    enabled: false
    api_key: ""

