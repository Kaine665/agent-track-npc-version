# 🚀 npm install 速度优化指南

> `npm install` 很慢？这里有几个快速解决方案！

## ⚡ 立即生效的优化（已应用）

### 1. 使用国内镜像（已启用）

已配置 `.npmrc` 使用国内镜像：
```
registry=https://registry.npmmirror.com
```

**速度提升**：10-15 分钟 → 1-3 分钟

---

## 🎯 立即执行（按顺序）

### 步骤 1：停止当前安装（如果还在运行）

按 `Ctrl+C` 停止

### 步骤 2：清理缓存和旧依赖

```bash
cd npc-frontend

# 清理 npm 缓存
npm cache clean --force

# 删除旧的 node_modules（如果存在）
rm -rf node_modules package-lock.json
```

### 步骤 3：重新安装（使用优化后的配置）

```bash
npm install
```

**预期时间**：1-3 分钟（之前可能需要 10+ 分钟）

---

## 🔧 如果还是慢（进一步优化）

### 方案 1：使用 cnpm（最快）

```bash
# 安装 cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com

# 使用 cnpm 安装
cd npc-frontend
cnpm install
```

### 方案 2：使用 pnpm（也很快）

```bash
# 安装 pnpm
npm install -g pnpm

# 使用 pnpm 安装
cd npc-frontend
pnpm install
```

### 方案 3：使用 yarn（备选）

```bash
# 安装 yarn
npm install -g yarn

# 使用 yarn 安装
cd npc-frontend
yarn install
```

---

## 📊 速度对比

| 方法 | 首次安装时间 | 使用缓存后 |
|------|-------------|-----------|
| npm（默认） | 10-15 分钟 | 5-10 分钟 |
| npm（国内镜像） | 1-3 分钟 | 30 秒-1 分钟 |
| cnpm | 1-2 分钟 | 20-30 秒 |
| pnpm | 1-2 分钟 | 20-30 秒 |
| yarn | 2-4 分钟 | 1-2 分钟 |

---

## 🔍 检查当前配置

### 查看当前镜像

```bash
npm config get registry
```

应该显示：`https://registry.npmmirror.com`

### 如果显示其他地址，手动设置

```bash
npm config set registry https://registry.npmmirror.com
```

### 验证配置

```bash
# 查看所有 npm 配置
npm config list
```

---

## 💡 其他优化技巧

### 1. 跳过可选依赖（如果不需要）

编辑 `.npmrc`，取消注释：
```
optional=false
```

### 2. 使用离线模式（如果已安装过）

```bash
npm install --prefer-offline
```

### 3. 只安装生产依赖（如果不需要开发依赖）

```bash
npm install --production
```

---

## 🐛 常见问题

### 问题 1：镜像无法访问

**解决**：尝试其他镜像

```bash
# 淘宝镜像（旧）
npm config set registry https://registry.npm.taobao.org

# 或者使用官方镜像（如果网络好）
npm config set registry https://registry.npmjs.org
```

### 问题 2：安装失败

**解决**：

```bash
# 清理缓存
npm cache clean --force

# 删除 node_modules
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 问题 3：权限问题

**解决**：

```bash
# Windows: 以管理员身份运行
# Linux/Mac: 使用 sudo（不推荐）或修复权限
```

---

## ✅ 推荐方案

### 开发环境（推荐）

```bash
# 使用 cnpm（最快）
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

### 生产环境（推荐）

```bash
# 使用 npm + 国内镜像（已配置）
npm install
```

---

## 📝 总结

**已应用的优化**：
- ✅ 使用国内镜像（`registry=https://registry.npmmirror.com`）
- ✅ 禁用审计（`audit=false`）
- ✅ 禁用进度显示（`progress=false`）
- ✅ 使用缓存（`prefer-offline=true`）
- ✅ 增加并行数（`maxsockets=15`）

**下一步**：
1. 清理缓存和旧依赖
2. 重新运行 `npm install`
3. 如果还是慢，使用 cnpm 或 pnpm

**预期效果**：
- 之前：10-15 分钟
- 现在：1-3 分钟

---

## 🆘 如果还是慢

告诉我：
1. 你的网络环境（国内/国外）
2. 当前使用的镜像（`npm config get registry`）
3. 具体的错误信息（如果有）

我可以进一步帮你优化！

